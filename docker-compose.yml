
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: nexus_backend
    restart: always
    environment:
      DATABASE_URL: postgresql://nexus_user:postgres@aci-database:5432/nexus
      SECRET_KEY: A8H8ARDZC8jnly5FjayIVEXhdLm4Dak2FlrAuFa2Vr2oh8TNYDESR2JIDT48hTpqiEk5jbaVsO-HlKb9YgZqUw
      SSO_SECRET_KEY: D4T_WY71xsF0_UB4QjIzlAjVlj-M5kEG0jsIws6isvPn5NNK4s5-_E_--WI6C6YT6jkerJ3EHncBEuG3tK5Rlg
      SMTP_SERVER: smtp.gmail.com
      SMTP_PORT: 587
      SMTP_USERNAME: your-email@gmail.com
      SMTP_PASSWORD: your-app-password
    ports:
      - "102:8000"
    volumes:
      - ./backend:/app
      - /app/__pycache__
    networks:
      - nexus_network
      - db-consolidation_aci-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: nexus_frontend
    restart: always
    ports:
      - "103:3000"
    depends_on:
      - backend
    environment:
      NEXT_PUBLIC_API_URL: http://acidashboard.aci.local:102
    networks:
      - nexus_network

  nginx:
    image: nginx:alpine
    container_name: nexus_nginx
    restart: always
    ports:
      - "0.0.0.0:100:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - frontend
      - backend
    networks:
      - nexus_network

networks:
  nexus_network:
    driver: bridge
  db-consolidation_aci-network:
    external: true
